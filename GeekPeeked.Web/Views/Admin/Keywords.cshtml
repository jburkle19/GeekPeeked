@model KeywordsViewModel

@{
    ViewBag.Title = "Keywords List";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 pull-right">
            <a href="@Url.Action("Index")" class="btn btn-block btn-default">Back to Admin</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <h2>GeekPeeked Keywords List (@Model.Keywords.Count results)</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="table-responsive">
                <table class="table table-striped results">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Movie Count</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var keyword in Model.Keywords.OrderByDescending(c => c.Movies.Count))
                        {
                            <tr>
                                <td>
                                    @keyword.Id
                                </td>
                                <td>
                                    @keyword.Name
                                </td>
                                <td>
                                    @keyword.Movies.Count
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

