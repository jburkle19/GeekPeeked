@model ProductionCompaniesViewModel

@{
    ViewBag.Title = "Production Companies List";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 pull-right">
            <a href="@Url.Action("Index")" class="btn btn-block btn-default">Back to Admin</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <h2>GeekPeeked Production Companies List (@Model.ProductionCompanies.Count results)</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="table-responsive">
                <table class="table table-striped results">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Movie Count</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var productionCompany in Model.ProductionCompanies.OrderByDescending(c => c.Movies.Count))
                        {
                            <tr>
                                <td>
                                    @productionCompany.Id
                                </td>
                                <td>
                                    @productionCompany.Name
                                </td>
                                <td>
                                    @productionCompany.Movies.Count
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

